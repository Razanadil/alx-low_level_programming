#include<stdio.h>
 /**
 * main - Entry point
 * Description: print the hexadecimal  
 * Return: o (success)
 */
int main(void)
{
Char n;
for (n = 0; n <= 9; n++)
putchar(n);
for (ch = 'a'; ch <= 'f'; ch++)
putchar(n);
putchar('\n');
return (0);
}

