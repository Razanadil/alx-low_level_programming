#include<stdio.h>
 /**
 * main - Entry point
 * Description: print the alphabet in lowercase
 * Return: o (success)
 */
int main (main)
{
    char ch;
     for (ch = 'a' , ch <= 'z' , ch++ )
          putchar(ch);
     putchar('\n');
                   return(0);
    }

